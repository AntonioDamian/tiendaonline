<UserControl x:Class="CapaPresentacionWPF.UscEstadisticas"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:local="clr-namespace:CapaPresentacionWPF"
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="1190" Loaded="UserControl_Loaded">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="23*"/>
            <ColumnDefinition Width="271*"/>
            <ColumnDefinition Width="345*"/>
            <ColumnDefinition Width="517*"/>
            <ColumnDefinition Width="34*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="25*"/>
            <RowDefinition Height="25*"/>
            <RowDefinition Height="200*"/>
            <RowDefinition Height="25*"/>

        </Grid.RowDefinitions>
        <Viewbox  Grid.Column="1"  Grid.Row="2" StretchDirection="UpOnly">
            <Calendar x:Name="CalendarioFechas" HorizontalAlignment="Center"  VerticalAlignment="Center" 
                 SelectionMode="SingleDate" SelectedDatesChanged="CalendarioFechas_SelectedDatesChanged"   />
        </Viewbox>

        <lvc:CartesianChart Grid.Row="2"  Grid.Column="2"  x:Name="BarraPedidos"  Series="{Binding ColunmSeriesCollection}" >
            <lvc:CartesianChart.AxisY>
                <lvc:Axis MinValue="0" Title="Cantidad" MaxValue="100">
                    <lvc:Axis.Separator>
                        <lvc:Separator IsEnabled="False" Step="1"/>
                    </lvc:Axis.Separator>
                </lvc:Axis>
            </lvc:CartesianChart.AxisY>
            <lvc:CartesianChart.AxisX >
                <lvc:Axis x:Name="cart_ejeX" Title="Productos" Labels="{Binding Labels}">
                    <lvc:Axis.Separator>
                        <lvc:Separator IsEnabled="True" Step="15"/>
                    </lvc:Axis.Separator>
                </lvc:Axis>
            </lvc:CartesianChart.AxisX>
        </lvc:CartesianChart>
        <lvc:PieChart x:Name="Piechart" Grid.Row="2" Grid.Column="3">

        </lvc:PieChart>




    </Grid>
</UserControl>
